global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'sws'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['spring:${MANAGEMENT_PORT}']